spring:
  application:
    name: gateway  #微服务名称
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848 # nacos地址
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml

  profiles:
    active: local
